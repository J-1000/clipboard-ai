{
  "name": "@clipboard-ai/actions",
  "version": "0.1.0",
  "description": "Built-in AI actions for clipboard-ai",
  "type": "module",
  "main": "builtin/index.js",
  "scripts": {
    "build": "bun build builtin/*.ts --outdir dist",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "openai": "^4.20.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.2"
  }
}

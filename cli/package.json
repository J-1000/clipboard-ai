{
  "name": "cbai",
  "version": "0.1.0",
  "description": "CLI for clipboard-ai - AI lookups & summaries triggered from your clipboard",
  "type": "module",
  "bin": {
    "cbai": "./dist/index.js"
  },
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target node",
    "dev": "bun run src/index.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "openai": "^4.20.0",
    "yargs": "^17.7.2"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@types/yargs": "^17.0.32",
    "typescript": "^5.3.2"
  }
}

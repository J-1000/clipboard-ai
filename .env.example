# clipboard-ai local HTTP API example environment

# Required: token matching settings.http_auth_token in ~/.clipboard-ai/config.toml
CBAI_HTTP_TOKEN=replace-with-your-token

# Optional: override API base URL if you changed settings.http_addr
CBAI_HTTP_BASE_URL=http://127.0.0.1:9159
